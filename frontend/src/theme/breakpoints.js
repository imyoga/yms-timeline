/**
 * Breakpoints configuration for the MUI theme
 * Defines responsive breakpoints for different screen sizes
 *
 * @example
 * import { breakpoints } from '../theme/breakpoints';
 * // Use breakpoints.values.md to access medium breakpoint value
 */

export const breakpoints = {
	values: {
		xs: 0,
		sm: 600,
		md: 900,
		lg: 1200,
		xl: 1536,
	},
	unit: 'px',
	step: 5,
}
